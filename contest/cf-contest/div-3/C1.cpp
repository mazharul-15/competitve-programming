#include<bits/stdc++.h>
using namespace std;

int main() {

    int t, n, i, A[5005];
    long long int p;
    set<int> st;
    map<int, int> mp;

    cin>> t;
    while(t--) {
        st.clear();
        mp.clear();
        cin>> n;
        for(i = 0; i < n; i++) cin>> A[i];
        for(i = 0; i < n; i++) {
            if(A[i] > 0)
        }
    }
    return 0;
}
